#include <stdio.h>
#include <iostream>
using namespace std;

int main(){
      int testCase = 0;
      int w, h;
      char** grid;

      scanf("%d", &testCase);
      for(int i = 0; i < testCase; i++){
            scanf("%d %d", &w, &h);
            
      }

      return 0;
}
